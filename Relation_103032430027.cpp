#include "Keyword.h"
#include "Paper.h"
#include <iostream>
#include <string>
using namespace std;


deleteRFirst_();
deleteRAfter_(adrR r, adrR prec, );
deleteRLast_();
findElmR_103032430027();


/*void deleteKeywordAfter(ListKeyword &K, adrKey prec, adrKey &p) {
    if (isEmpty(K)) {
        p = nullptr;
        cout << "List Kosong";
    } else if (K.first -> next = nullptr) {
        p = K.first;
        K.first = nullptr;
    } else {
        p = prec -> next;
        prec -> next = p -> next;
        p -> next = nullptr;
    }
}*/
